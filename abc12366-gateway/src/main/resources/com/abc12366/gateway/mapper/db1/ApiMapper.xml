<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.gateway.mapper.db1.ApiMapper">

    <insert id="insert" parameterType="Api">
        insert into api (id, name, mark, method, role, version, appId, status, createDate, modifyDate)
        values (#{id}, #{name}, #{mark}, #{method}, #{role}, #{version}, #{appId}, #{status}, #{createDate}, #{modifyDate})
    </insert>

    <update id="update">
        update api
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="mark != null">mark = #{mark},</if>
            <if test="method != null">method = #{method},</if>
            <if test="role != null">role = #{role},</if>
            <if test="version != null">version = #{version},</if>
            <if test="appId != null">appId = #{appId},</if>
            <if test="status != null">status = #{status},</if>
            <if test="modifyDate != null">modifyDate = #{modifyDate}</if>
        </set>
        where id=#{id}
    </update>
</mapper>