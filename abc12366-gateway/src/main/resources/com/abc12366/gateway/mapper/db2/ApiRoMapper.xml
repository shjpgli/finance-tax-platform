<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.gateway.mapper.db2.ApiRoMapper">
    <select id="selectList" resultType="com.abc12366.gateway.model.Api">
        select id, name, uri, method, version, appId, authentication, status, createTime, lastUpdate
        from gw_api
        <where>
            <if test="appId != null">appId = #{appId}</if>
            <if test="uri != null">AND uri = #{uri}</if>
            <if test="method != null">AND method = #{method}</if>
            <if test="version != null">AND version = #{version}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <select id="selectOne" resultType="com.abc12366.gateway.model.Api">
        select id, name, uri, method, version, appId, authentication, status, createTime, lastUpdate
        from gw_api
        <where>
            <if test="appId != null">appId = #{appId}</if>
            <if test="uri != null">AND uri = #{uri}</if>
            <if test="method != null">AND method = #{method}</if>
            <if test="version != null">AND version = #{version}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>
</mapper>