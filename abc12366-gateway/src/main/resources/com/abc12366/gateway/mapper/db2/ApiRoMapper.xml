<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.gateway.mapper.db2.ApiRoMapper">

    <select id="selectList" resultType="Api">
        select id, name, mark, method, role, version, appId, status, createDate, modifyDate
        from api
    </select>

    <select id="selectOne" resultType="Api">
        select id, name, mark, method, role, version, appId, status, createDate, modifyDate
        from api
        where id = #{id}
    </select>
</mapper>