<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.gateway.mapper.db1.AppMapper">
    <insert id="insert" parameterType="App">
        insert into app (id, name, password, mark, status, createDate, modifyDate)
        values (#{id}, #{name}, #{password}, #{mark}, #{status}, #{createDate}, #{modifyDate}
    </insert>
</mapper>