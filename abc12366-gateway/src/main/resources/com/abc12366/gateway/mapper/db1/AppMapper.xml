<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.gateway.mapper.db1.AppMapper">
    <insert id="insert" parameterType="App">
        insert into gw_app (id, name, password, accessToken, lastResetTokenTime, startTime, endTime, remark, status, createTime, lastUpdate)
        values (#{id}, #{name}, #{password}, #{accessToken}, #{lastResetTokenTime}, #{startTime}, #{endTime},
                #{remark}, #{status}, #{createTime}, #{lastUpdate})
    </insert>

    <update id="update" parameterType="App">
        UPDATE gw_app
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="accessToken != null">accessToken = #{accessToken},</if>
            <if test="lastResetTokenTime != null">lastResetTokenTime = #{lastResetTokenTime},</if>
            <if test="startTime != null">startTime = #{startTime},</if>
            <if test="endTime != null">endTime = #{endTime},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastUpdate != null">lastUpdate = #{lastUpdate}</if>
        </set>
        where id=#{id}
    </update>
</mapper>