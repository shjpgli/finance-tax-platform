<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.cms.mapper.db2.SiteRoMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		siteId,	domain,	sitePath,	siteName,	shortName,	protocol,	dynamicSuffix,	staticSuffix,	staticDir,	isIndexToRoot,	isStaticIndex,	localeAdmin,	localeFront,	tplSolution,	finalStep,	afterCheck,	isRelativePath,	isRecycleOn,	domainAlias,	domainRedirect,	tplIndex,	keywords,	description,	siteLogo,	copyrightExplain,	recordNum,	resourceSync,	staticSync,	staticIssue,	siteStatus
	</sql>
	
	<!-- 查询（根据主键ID查询） -->
	<select id="selectByPrimaryKey" resultType="com.abc12366.cms.model.Site" parameterType="java.lang.String">
		 SELECT
		 <include refid="Base_Column_List" />
		 FROM cms_site
		 WHERE siteId = #{siteId}
	</select>

	<!-- 查询所有 -->
	<select id="selectList" resultType="com.abc12366.cms.model.bo.SiteListBo" parameterType="java.lang.String">
		SELECT
		<include refid="Base_Column_List" />
		FROM cms_site
	</select>



</mapper>