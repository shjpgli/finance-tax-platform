<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.cms.mapper.db2.EventBangBangMapper">

	<select id="singleEvent" resultType="com.abc12366.cms.model.event.SingleEventBo" >
		SELECT a.eventId,a.province,a.city,a.address,a.picture,a.begintime,a.endtime,a.peoppleNum,b.sponsorName,a.`bmendtime`,a.title
		FROM `cms_event` a LEFT JOIN cms_event_sponsor b ON a.sponsorId = b.sponsorId
		WHERE STATUS=2 ORDER BY createtime DESC limit 1
	</select>

	<select id="singleEventList" resultType="com.abc12366.cms.model.event.SingleEventBo" >
		SELECT a.eventId,a.province,a.city,a.address,a.picture,a.begintime,a.endtime,a.peoppleNum,b.sponsorName,a.`bmendtime`,a.title
		FROM `cms_event` a LEFT JOIN cms_event_sponsor b ON a.sponsorId = b.sponsorId
		WHERE STATUS in (2,3) ORDER BY createtime DESC
	</select>

</mapper>