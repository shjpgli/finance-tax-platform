<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.cms.mapper.db2.CurriculumApplyRoMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 applyId,	curriculumId,	userId,	nickname,	username,	applyTime,	phone,	signTime,	signIP,	signSite
	</sql>

	<!-- 查询（根据主键ID查询） -->
	<select id="selectByPrimaryKey" resultType="com.abc12366.cms.model.curriculum.CurriculumApply" parameterType="java.lang.String">
		 SELECT
		 <include refid="Base_Column_List" />
		 FROM cms_curriculum_apply
		 WHERE applyId = #{applyId}
	</select>

    <!-- 查询（根据主键ID查询） -->
    <select id="selectList" resultType="com.abc12366.cms.model.curriculum.bo.CurriculumApplyBo" parameterType="java.util.Map">
        SELECT
        <include refid="Base_Column_List" />
        FROM cms_curriculum_apply
        WHERE 1=1
    </select>


</mapper>