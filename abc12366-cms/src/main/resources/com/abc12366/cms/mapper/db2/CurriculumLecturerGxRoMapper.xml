<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.cms.mapper.db2.CurriculumLecturerGxRoMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 curriculumId,lecturerId,lecturerName,	lecturerPicture
	</sql>

	<!-- 查询（根据主键ID查询） -->
	<select id="selectByPrimaryKey" resultType="com.abc12366.cms.model.curriculum.CurriculumLecturerGx" parameterType="java.lang.String">
		 SELECT
             curriculumId,lecturerId
		 FROM cms_curriculum_lecturer_gx
		 WHERE lecturerId = #{lecturerId}
	</select>

    <!-- 查询（根据主键ID查询） -->
    <select id="selectList" resultType="com.abc12366.cms.model.curriculum.CurriculumLecturerGx" parameterType="java.lang.String">
        SELECT
            a.curriculumId,a.lecturerId,b.lecturerName,	b.lecturerPicture
        FROM cms_curriculum_lecturer_gx a LEFT JOIN cms_curriculum_lecturer b on a.lecturerId = b.lecturerId
        WHERE curriculumId = #{curriculumId}
    </select>


</mapper>