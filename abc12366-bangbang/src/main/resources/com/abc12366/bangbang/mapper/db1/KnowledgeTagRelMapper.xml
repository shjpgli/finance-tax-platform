<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.bangbang.mapper.KnowledgeTagRelMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 id,	knowledgeId,	tagId
	</sql>

	<!-- 查询（根据主键ID查询） -->
	<select id="selectByPrimaryKey" resultType="knowledgeTagRel" parameterType="java.lang.String">
		 SELECT
		 <include refid="Base_Column_List" />
		 FROM bb_knowledge_tag_rel
		 WHERE id = #{id}
	</select>

	<!--删除：根据主键ID删除-->
	<delete id="deleteByPrimaryKey" parameterType="java.lang.String">
		 DELETE FROM bb_knowledge_tag_rel
		 WHERE id = #{id}
	</delete>

	<!-- 添加 -->
	<insert id="insert" parameterType="knowledgeTagRel">
		 INSERT INTO bb_knowledge_tag_rel
 		(id,knowledgeId,tagId) 
		 VALUES 
 		(#{id},#{knowledgeId},#{tagId}) 
	</insert>

	<!-- 添加 -->
	<insert id="insertBatch" parameterType="java.util.List">
		INSERT INTO bb_knowledge_tag_rel
		(id,knowledgeId,tagId)
		VALUES
		<foreach collection="list" item="item" index="index" separator="," >
			(#{item.id},#{item.knowledgeId},#{item.tagId})
		</foreach>
	</insert>


	<!-- 添加 （匹配有值的字段）-->
	<insert id="insertSelective" parameterType="knowledgeTagRel">
		 INSERT INTO bb_knowledge_tag_rel
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="id != null">
				 id,
			</if>
			<if test="knowledgeId != null">
				 knowledgeId,
			</if>
			<if test="tagId != null">
				 tagId,
			</if>

		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="id!=null">
				 #{id},
			</if>
			<if test="knowledgeId!=null">
				 #{knowledgeId},
			</if>
			<if test="tagId!=null">
				 #{tagId},
			</if>
		 </trim>
	</insert>

	<!-- 修 改-->
	<update id="updateByPrimaryKeySelective" parameterType="knowledgeTagRel">
		 UPDATE bb_knowledge_tag_rel
 		 <set> 
			<if test="knowledgeId != null">
				 knowledgeId = #{knowledgeId},
			</if>
			<if test="tagId != null">
				 tagId = #{tagId},
			</if>

 		 </set>
		 WHERE id = #{id}
	</update>

	<!-- 修 改-->
	<update id="updateByPrimaryKey" parameterType="knowledgeTagRel">
		 UPDATE bb_knowledge_tag_rel
		 SET 
			 knowledgeId = #{knowledgeId},
			 tagId = #{tagId}
		 WHERE id = #{id}
	</update>

</mapper>