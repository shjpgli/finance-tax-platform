<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.admin.mapper.db2.UserRoMapper">

	<resultMap id="userBoResultMap" type="com.abc12366.admin.model.bo.UserBO">
		<id column="id" property="id" jdbcType="VARCHAR"/>
		<result column="username" property="username" jdbcType="VARCHAR"/>
		<result column="password" property="password" jdbcType="VARCHAR"/>
		<result column="nickname" property="nickname" jdbcType="VARCHAR"/>
		<result column="status" property="status" jdbcType="TINYINT"/>
		<result column="createTime" property="createTime" jdbcType="TIMESTAMP"/>
		<result column="lastUpdate" property="lastUpdate" jdbcType="TIMESTAMP"/>
		<result column="organization_id" property="organizationId" jdbcType="INTEGER"/>
		<result column="organizationName" property="organizationName" jdbcType="VARCHAR"/>

		<collection property="rolesList" ofType="Role">
			<id column="roleId" property="id"/>
			<result column="roleName" property="name"/>
		</collection>
	</resultMap>

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 id,	username,	password,	nickname,	status,	createTime,	lastUpdate
	</sql>

	<!-- 查询（根据主键ID查询） -->
	<select id="selectByPrimaryKey" resultType="User" parameterType="java.lang.String">
		 SELECT
		 <include refid="Base_Column_List" />
		 FROM sys_user
		 WHERE id = #{id}
	</select>

	<!-- 查询所有用户 -->
	<select id="selectList" resultType="User">
		SELECT
		<include refid="Base_Column_List" />
		FROM sys_user
	</select>

    <!--查询单个用户及权限-->
   <!-- <select id="selectOne" resultMap="userBoResultMap" parameterType="User">
        SELECT
        t.id,
        t.username,
        t.password,
        t.nickname,
        t.status,
        t.token,
        t.lastResetTokenTime,
        t.createTime,
        t.lastUpdate,
        t.organization_id,
        s.name AS organizationName,
        o.id AS roleId,
        o.roleName AS roleName
        FROM
        sys_user t
        LEFT JOIN sys_user_role r ON t.id = r.userId
        LEFT JOIN sys_role o ON r.roleId = o.id
        LEFT JOIN sys_organization s ON s.id = t.organizationId
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="userName != null">userName = #{userName}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>
-->
    <!--根据条件查找单个用户-->
    <select id="selectOne" resultType="User" parameterType="User">
        select
        <include refid="Base_Column_List"/>
        from sys_user
        where id = #{id}
    </select>

	<select id="selectUserByLoginName" resultType="User" parameterType="java.lang.String">
		select
		<include refid="Base_Column_List"/>
		from sys_user
		where username = #{username}
	</select>

	<select id="selectUserById" resultType="User" parameterType="java.lang.Long">
		select
		<include refid="Base_Column_List"/>
		from sys_user
		where id = #{id}
	</select>

	<select id="selectUserBoById" resultMap="userBoResultMap" parameterType="java.lang.String">
		SELECT
		t.id,
		t.username,
		t.password,
		t.nickname,
		t.status,
		t.createTime,
		t.lastUpdate,
		t.organization_id,
		s.name AS organizationName,
		o.id AS roleId,
		o.roleName AS roleName
		FROM
		sys_user t
		LEFT JOIN sys_user_role r ON t.id = r.userId
		LEFT JOIN sys_role o ON r.roleId = o.id
		LEFT JOIN sys_organization s ON s.id = t.organizationId
		<where>
			t.id = #{id}
		</where>
	</select>


</mapper>