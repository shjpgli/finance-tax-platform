<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.admin.mapper.db1.DictMapper">

    <insert id="insert" parameterType="Dict">
        insert into sys_dict (id, dictId, dictName, fieldKey, fieldValue, status, createTime, createUser, lastUpdate, lastUser)
        values (#{id}, #{dictId}, #{dictName}, #{fieldKey}, #{fieldValue}, #{status}, #{createTime}, #{createUser}, #{lastUpdate}, #{lastUser})
    </insert>

    <update id="update">
        update sys_dict
        <set>
            <if test="dictId != null">dictId = #{dictId},</if>
            <if test="dictName != null">dictName = #{dictName},</if>
            <if test="fieldKey != null">fieldKey = #{fieldKey},</if>
            <if test="fieldValue != null">fieldValue = #{fieldValue},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastUpdate != null">lastUpdate = #{lastUpdate},</if>
            <if test="lastUser != null">lastUser = #{lastUser}</if>
        </set>
        where id=#{id}
    </update>

    <delete id="delete">
        delete from sys_dict
        where id=#{id}
    </delete>
</mapper>