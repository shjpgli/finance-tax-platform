<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.admin.mapper.db2.RoleRoMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 id,	roleName,	remark,	createTime,	updateTime,	status
	</sql>

	<!-- 查询（根据主键ID查询） -->
	<select id="selectByPrimaryKey" resultType="Role" parameterType="java.lang.String">
		 SELECT
		 <include refid="Base_Column_List" />
		 FROM sys_role
		 WHERE id = #{id}
	</select>

	<select id="selectList" resultType="Role" parameterType="Role">
		select
		<include refid="Base_Column_List"/>
		from sys_role
        <where>
            1=1
            <if test="roleName != null and roleName!=''"> and roleName LIKE CONCAT('%',#{roleName},'%') </if>
            <if test="status != null and status!=''"> AND status = #{status}</if>
        </where>
	</select>

	<select id="selectRoleById" resultType="Role" parameterType="java.lang.String">
		select
		<include refid="Base_Column_List"/>
		from sys_role
		where id = #{id,jdbcType=BIGINT}
	</select>

    <select id="selectOne" resultType="Role" parameterType="Role">
        select
        <include refid="Base_Column_List"/>
        from sys_role
        <where>
            <if test="id != null and id!=''">AND id = #{id}</if>
        </where>
    </select>

	<select id="selectMenuIdListByRoleId" resultType="java.lang.String" parameterType="java.lang.String">
		SELECT
		e.menuId AS id
		FROM
		sys_role r
		LEFT JOIN sys_role_menu e ON r.id = e.roleId
		WHERE
		r.id = #{id}
	</select>

	<select id="selectRoleMenuIdListByRoleId" resultType="java.lang.String" parameterType="java.lang.String">
		SELECT
		e.id AS id
		FROM
		sys_role r
		LEFT JOIN sys_role_menu e ON r.id = e.roleId
		WHERE
		r.id = #{id}
	</select>

	<select id="selectRoleMenuListByRoleId" resultType="java.util.HashMap" parameterType="java.lang.String">
		SELECT
		e.id AS id,
		s.MenuUrl AS url
		FROM
		sys_role r
		LEFT JOIN sys_role_menu e ON r.id = e.roleId
		LEFT JOIN sys_menu s ON e.menuId = s.menuId
		WHERE
		r.id = #{id}
	</select>

</mapper>