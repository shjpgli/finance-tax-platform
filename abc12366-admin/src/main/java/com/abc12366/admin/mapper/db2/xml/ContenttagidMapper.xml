<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.admin.mapper.db2.ContenttagidMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 id,	tagId,	priority
	</sql>

	<!-- 查询（根据主键ID查询） -->
	<select id="selectByPrimaryKey" resultType="contenttagid" parameterType="java.lang.String">
		 SELECT
		 <include refid="Base_Column_List" />
		 FROM cms_contenttagid
		 WHERE contentId = #{contentId}
	</select>

	<!--删除：根据主键ID删除-->
	<delete id="deleteByPrimaryKey" parameterType="java.lang.String">
		 DELETE FROM cms_contenttagid
		 WHERE contentId = #{contentId}
	</delete>

	<!-- 添加 -->
	<insert id="insert" parameterType="contenttagid">
		 INSERT INTO cms_contenttagid
 		(contentId,tagId,priority) 
		 VALUES 
 		(#{contentId},#{tagId},#{priority}) 
	</insert>

	<!-- 添加 （匹配有值的字段）-->
	<insert id="insertSelective" parameterType="contenttagid">
		 INSERT INTO cms_contenttagid
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="contentId != null">
				 contentId,
			</if>
			<if test="tagId != null">
				 tagId,
			</if>
			<if test="priority != null">
				 priority,
			</if>

		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="contentId!=null">
				 #{contentId},
			</if>
			<if test="tagId!=null">
				 #{tagId},
			</if>
			<if test="priority!=null">
				 #{priority},
			</if>
		 </trim>
	</insert>

	<!-- 修 改-->
	<update id="updateByPrimaryKeySelective" parameterType="contenttagid">
		 UPDATE cms_contenttagid
 		 <set> 
			<if test="tagId != null">
				 tagId = #{tagId},
			</if>
			<if test="priority != null">
				 priority = #{priority},
			</if>

 		 </set>
		 WHERE contentId = #{contentId}
	</update>

	<!-- 修 改-->
	<update id="updateByPrimaryKey" parameterType="contenttagid">
		 UPDATE cms_contenttagid
		 SET 
			 tagId = #{tagId},
			 priority = #{priority}
		 WHERE contentId = #{contentId}
	</update>

</mapper>