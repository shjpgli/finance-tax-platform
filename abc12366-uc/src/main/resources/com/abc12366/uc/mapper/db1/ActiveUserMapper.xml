<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.uc.mapper.db1.ActiveUserMapper">

    <select id="selectList" resultType="ActiveUser">
        select id, username, connectionTime from active_user
    </select>

    <select id="selectOne" resultType="ActiveUser">
        select id, username, connectionTime from active_user
        where id = #{id}
    </select>

    <select id="delete">
        DELETE from active_user
        where id = #{id}
    </select>

    <insert id="insert" parameterType="User">
        insert into active_user (id, username, connectionTime)
        values (#{id}, #{username}, #{connectionTime})
    </insert>
</mapper>