<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.uc.mapper.db2.UserExtendRoMapper">

    <select id="selectOne" resultType="com.abc12366.uc.model.UserExtend">
        select userId,signature,sex,birthday,bloodType,weight,height,marital, education,graduate, occupation,income,postAddress,realName, weixin,qq,safeQuestion,safeAnswer,province,city,area,createTime, lastUpdate,tags,idcard,frontImage,backImage,startTime,endTime,validStatus,validTime,remark
        from uc_user_extend        
        where userId = #{userId}
    </select>

    <select id="selectList" resultType="com.abc12366.uc.model.bo.UserExtendListBO" parameterType="java.util.Map">
        select uu.username,uu.nickname,uu.phone,uu.status,
        uue.userId,uue.signature,uue.sex,uue.birthday,uue.bloodType,uue.weight,uue.height,uue.marital,
        uue.education,uue.graduate, uue.occupation,uue.income,uue.postAddress,uue.realName,
        uue.weixin,uue.qq,uue.safeQuestion,
        uue.safeAnswer,uue.province,uue.city,uue.area,uue.createTime,
        uue.lastUpdate,uue.tags,uue.idcard,uue.frontImage,uue.backImage,
        uue.startTime,uue.endTime,uue.validStatus,uue.validTime,uue.remark
        from uc_user_extend uue, uc_user uu
        <where>
            uue.userId=uu.id
            <if test="username!=null">
                and uu.username like CONCAT('%',#{username},'%')
            </if>
        </where>

    </select>

</mapper>