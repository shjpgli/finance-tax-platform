<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.uc.mapper.db2.UserRoMapper">

    <select id="selectList" resultType="UserBO">
        select id, username, phone, password, regMail, regIP, salt, nickname, status, createTime, lastUpdate,
        userPicturePath, maxUserPicturePath, midUserPicturePath, minUserPicturePath, points, exp, vipLevel
        from uc_user
        ORDER BY lastUpdate DESC
    </select>

    <select id="selectOne" resultType="User">
        select id, username, phone, password, regMail, regIP, salt, nickname, status, createTime, lastUpdate,
        userPicturePath, maxUserPicturePath, midUserPicturePath, minUserPicturePath, points, exp, vipLevel
        from uc_user
        where id = #{id}
    </select>

    <select id="selectByUsernameOrPhone" resultType="User">
        select id, username, phone, password, regMail, regIP, salt, nickname, status, createTime, lastUpdate,
        userPicturePath, maxUserPicturePath, midUserPicturePath, minUserPicturePath, points, exp, vipLevel
        from uc_user
        <where>
            <if test="username != null">
                username = #{username}
            </if>
            <if test="phone != null">
                OR phone = #{phone}
            </if>
        </where>
    </select>

</mapper>