<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.uc.mapper.db2.UserRoMapper">

    <select id="selectList" resultType="User">
        select id, username, password, phone, enabled, lastPasswordResetDate, createDate, modifyDate,
          authority as roles
        from user a
        left join authority b on a.id = b.userId
    </select>

    <select id="selectOne" resultType="User">
        select id, username, password, phone, enabled, lastPasswordResetDate, createDate, modifyDate,
        authority as roles
        from user a
        left join authority b on a.id = b.userId
        where id = #{id}
    </select>

    <select id="selectByUsernameOrPhone" resultType="User">
        select id, username, password, phone, enabled, lastPasswordResetDate, createDate, modifyDate,
        authority as roles
        from user a
        left join authority b on a.id = b.userId
        where username = #{username}
        or phone = #{phone}
    </select>

</mapper>