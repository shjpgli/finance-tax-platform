<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc12366.uc.mapper.db2.UserRoMapper">

    <select id="selectList" resultType="com.abc12366.uc.model.bo.UserBO" parameterType="java.util.Map">
        select uu.id, uu.username, uu.phone, uu.password, uu.regMail, uu.regIP, uu.salt, uu.nickname, uu.status, uu.createTime, uu.lastUpdate,
        uu.userPicturePath, uu.maxUserPicturePath, uu.midUserPicturePath, uu.minUserPicturePath, uu.points, uu.exp, uu.vipLevel,
        (select uue.realName from uc_user_extend uue where uue.userId=uu.id) realName
        from uc_user uu, uc_tag ut, uc_subject_tag ust
        <where>
            <if test="username!=null">
               username=#{username}
            </if>
            <if test="phone!=null">
                and phone=#{phone}
            </if>
            <if test="nickname!=null">
                and nickname=#{nickname}
            </if>
            <if test="status!=null">
                and status=#{status}
            </if>
            <if test="tagName!=null">
                and uu.id = ust.userId
                and ust.tagId=ut.id
                and ut.tagName=#{tagName}
            </if>
        </where>
        ORDER BY lastUpdate DESC
    </select>

    <select id="selectOne" resultType="com.abc12366.uc.model.User" parameterType="java.lang.String">
        select id, username, phone, password, regMail, regIP, salt, nickname, status, createTime, lastUpdate,
        userPicturePath, maxUserPicturePath, midUserPicturePath, minUserPicturePath, points, exp, vipLevel
        from uc_user
        where id = #{id}
    </select>

    <select id="selectByUsernameOrPhone" resultType="com.abc12366.uc.model.User" parameterType="com.abc12366.uc.model.bo.LoginBO">
        select id, username, phone, password, regMail, regIP, salt, nickname, status, createTime, lastUpdate,
        userPicturePath, maxUserPicturePath, midUserPicturePath, minUserPicturePath, points, exp, vipLevel
        from uc_user
        <where>
            <if test="usernameOrPhone != null">
                username = #{usernameOrPhone}
            </if>
            <if test="usernameOrPhone != null">
                OR phone = #{usernameOrPhone}
            </if>
        </where>
    </select>

</mapper>